using System.Collections;
using System.Collections.Generic;
using GunGame.Guns;
using UnityEngine;
/* 
public class BurstFire : FireMode
{
   
    private int burstLeft;
    // Start is called before the first frame update
    
 
    
    // Fires a specified amount of bullets every time the trigger is pulled
    public override void FireType(GunStats gunStats, int burst, float fireRate)
    
    {
        if(gunStats.burstTime <= 0)
        {
            if (Input.GetMouseButtonDown(0))
            {
                burstLeft = gunStats.burstSize;
                gunStats.burstTime = gunStats.burstDelay;

                if (burstLeft > 0)
                {
                    gunStats.FireBullet();
                    burstLeft--;
                }
                        

            }
        }
       
       
    }
    
}
*/
